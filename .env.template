# Environment variables for Docker Compose stack
# This file should be copied to .env and customized

# User and Group IDs (auto-detected by bootstrap_mac.sh)
# Run `id -u` to get your user ID
# Run `id -g` to get your group ID (typically 20 = 'staff' on macOS)
PUID=501
PGID=20

# Timezone (adjust as needed)
TZ=Europe/Zurich

# Plex claim token (optional)
# Get from: https://www.plex.tv/claim
# Valid for 4 minutes after generation
PLEX_CLAIM=

# Base path for all data (mounted external volume)
DATA_PATH=/Volumes/space
